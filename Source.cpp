#include "Header.h"

int main(int argc, char* const argv[]) {
	//TestQueue();
	/*int m = 4;
	int n = 6;
	vector< vector<int>> Matrix(m, vector<int>(n));
	srand(time(NULL));
	LOOP(i, m) {
		LOOP(j, n) {
			Matrix[i][j] = (rand() % 2);
		}
	}
	LOOP(i, m) {
		LOOP(j, n) {
			cout << Matrix[i][j] << '\t';
		}
		cout << endl;
	}
	struct point
	{
		int key;
		int x;
		int y;
	};
	vector<point> S;
	vector<point> Q;
	point a{ 1, 2 };
	point b{ 4, 9 };
	int x1 = 2;
	int y1 = 6;
	bool con0 = (x1 + 1 >= 0 && x1 + 1 < n && y1 >= 0 && y1 < m);
	bool con1 = (x1 >= 0 && x1 < n && y1 - 1 >= 0 && y1 - 1 < m);
	bool con2 = (x1 - 1 >= 0 && x1 - 1 < n && y1 >= 0 && y1 < m);
	bool con3 = (x1 >= 0 && x1 < n && y1 + 1 >= 0 && y1 + 1 < m);
	point E = { 1, x1, y1 };
	point F;
	point L={1, 6, 2};
	S.push_back(E);
	bool con[] = { con0, con1, con2, con3 };
	LOOP(i, m) {
		LOOP(index, sizeof(con) / sizeof(con1)) {
			if (con[index]) {
				F = {1,  x1 + 1, y1 + 1 };
				if (Matrix[F.x][F.y] == 1) {
					S.push_back(F);
				
				}
			}
		}
		LOOP(j, n) {

		}
	}*/
	/*int t = 0;
	if (t++ && 1);
	cout << "t = " << t;
	cout << "\n\n\n";*/
	//TestMain();
	/*int* a = new int[2];
	a[0] = 2;
	a[1] = 3;
	a = (int*)realloc(a, 12);
	a[2] = 11;
	cout << a[1];*/
	//DemoCalTime();
	//int nA = 5;// 0000 0101
	//int nB = 6;// 0000 0110
	//int nZ1, nZ2, nZ3, nZ4, nZ5, nZ6;
	//nZ1 = nA & nB;// 0000 0100
	//nZ2 = nA | nB;// 0000 0111
	//nZ3 = nA ^ nB;// 0000 0011
	//nZ4 = ~nA;// 1111 1010
	//nZ5 = nA >> 2; // 0000 0001
	//nZ6 = nA << 2; // 0001 0100
	//cout << "nZ5 = " << nZ5 << endl;
	//cout << "nZ6 = " << nZ6 << endl;
	system("pause");
	return 0;
}
